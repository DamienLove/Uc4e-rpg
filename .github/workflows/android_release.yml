name: Android Release (APK)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Flutter (latest stable)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.6'
          channel: 'stable'

      - name: Flutter version
        run: flutter --version

      # If you already have your Flutter project folder (e.g. universe_connected), skip this step
      # - name: Create Flutter skeleton (one-time)
      #   run: flutter create universe_connected

      - name: Ensure Android SDK version
        run: |
          FILE=universe_connected/android/app/build.gradle
          if [ -f "$FILE" ]; then
            sed -i 's/
