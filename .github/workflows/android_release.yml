name: Android Release (APK)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Flutter (latest stable)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.6'
          channel: 'stable'

      - name: Flutter version
        run: flutter --version

      # Optional: only needed if your app folder doesn’t exist yet
      # - name: Create Flutter skeleton (one-time)
      #   run: flutter create universe_connected

      - name: Ensure Android SDK version
        shell: bash
        run: |
          FILE="universe_connected/android/app/build.gradle"
          if [[ -f "$FILE" ]]; then
            echo "Patching compileSdk in $FILE"
            sed -i "s/compileSdk.*/compileSdk = 35/" "$FILE" || true
          else
            echo "No build.gradle found at $FILE"
          fi

      - name: Get packages
        working-directory: universe_connected
        run: flutter pub get

      - name: Build release APK
        working-directory: universe_connected
        run: flutter build apk --release

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: uc4e-apk
          path: universe_connected/build/app/outputs/flutter-apk/app-release.apk
